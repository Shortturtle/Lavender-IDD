export interface SRect {
    height: number;
    width: number;
    x: number;
    y: number;
    assumeClientRect: boolean;
    normalized: boolean;
}
export interface SnipOptions {
    getDataUrl?: boolean;
    mimeType?: string;
    rect?: SRect;
    scale?: number;
    timeout?: number;
    displayPixelRatio?: number;
    cloneCanvas?: boolean;
}
export declare class CanvasSnipper {
    static Debug: boolean;
    static GetClonedCanvas(canvas: HTMLCanvasElement, { rect, displayPixelRatio, scale, }: SnipOptions): Promise<HTMLCanvasElement>;
    static GetDataUrl(canvas: HTMLCanvasElement, { mimeType, ...options }: SnipOptions): Promise<string>;
    static GetImage(canvas: HTMLCanvasElement, { mimeType, ...options }?: SnipOptions): Promise<HTMLImageElement>;
    static GetBlob(canvas: HTMLCanvasElement, options?: SnipOptions): Promise<Blob>;
    static GetFile(canvas: HTMLCanvasElement, filename?: string, options?: SnipOptions): Promise<File | string>;
    static GetTiledFiles(canvas: HTMLCanvasElement, filePrefix?: string, tileRows?: number, tileCols?: number, options?: SnipOptions): Promise<(File | string)[]>;
}
//# sourceMappingURL=canvasSnipper.d.ts.map