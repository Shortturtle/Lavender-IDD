import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
import { AssetManagerPlugin } from 'webgi/extras/asset_manager/AssetManagerPlugin';
import { Object3D } from 'three';
import { UiObjectConfig } from 'webgi/interfaces';
import { ObjectSwitchNode } from 'webgi/plugins/SwitchNodePlugin';
export declare abstract class SwitchNodeBasePlugin extends AViewerPlugin<''> {
    enabled: boolean;
    private _picking;
    private _uiNeedRefresh;
    constructor();
    private _isVisibleChanged;
    onAdded(viewer: ViewerApp): Promise<void>;
    onRemove(viewer: ViewerApp): Promise<void>;
    private _postFrame;
    private _preRender;
    private _postRender;
    refreshUi(): void;
    /**
     * If true, the plugin will automatically take snapshots of the icons in _refreshUi and put them in the object.userdata.__icon
     * Otherwise, call {@link snapIcons} manually
     */
    autoSnapIcons: boolean;
    protected _refreshUi(): boolean;
    /**
     * Snapshots icons and puts in the userdata.__icon
     */
    snapIcons(): void;
    dependencies: (typeof AssetManagerPlugin)[];
    variations: ObjectSwitchNode[];
    protected _selectedSwitchNode: () => Object3D | undefined;
    uiConfig: UiObjectConfig;
}
//# sourceMappingURL=SwitchNodeBasePlugin.d.ts.map