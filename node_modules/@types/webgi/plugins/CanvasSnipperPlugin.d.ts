import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { SnipOptions } from 'webgi/extras/canvasSnipper';
import { Vector4 } from 'three';
export type CanvasSnipperOptions = SnipOptions & {
    /**
     * If true, will wait for progressive rendering(requires {@link ProgressivePlugin}) to finish before taking snapshot
     * @default true
     */
    waitForProgressive?: boolean;
    /**
     * Number of progressive frames to wait for before taking snapshot
     @default 64 or {@link ProgressivePlugin.maxFrameCount}, whichever is higher
     */
    progressiveFrames?: number;
    /**
     * Number of tile rows to split the image into
     * @default 1
     */
    tileRows?: number;
    /**
     * Number of tile columns to split the image into
     */
    tileColumns?: number;
};
export declare class CanvasSnipperPlugin extends AViewerPlugin<''> {
    static readonly PluginType = "CanvasSnipper";
    enabled: boolean;
    constructor();
    getFile(filename?: string, options?: CanvasSnipperOptions): Promise<File | undefined>;
    getDataUrl(options?: SnipOptions): Promise<string>;
    private _getFile;
    filename: string;
    progressiveFrames: number;
    tileRows: number;
    tileColumns: number;
    rect: Vector4;
    private _downloading;
    downloadSnapshot(filename?: string, options?: CanvasSnipperOptions): Promise<void>;
    protected _downloadJpeg(): Promise<void>;
    protected _downloadWebp(): Promise<void>;
    private _downloadBlob;
}
//# sourceMappingURL=CanvasSnipperPlugin.d.ts.map