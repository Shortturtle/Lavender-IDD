import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { BufferGeometry, Mesh, PlaneGeometry } from 'three';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export declare class WaveGroundPlugin extends AViewerPlugin<''> {
    static readonly PluginType = "WaveGroundPlugin";
    enabled: boolean;
    private readonly _planeMesh;
    planeMesh: Mesh<PlaneGeometry>;
    constructor();
    onAdded(viewer: ViewerApp): Promise<void>;
    onEnable(): Promise<void>;
    onRemove(viewer: ViewerApp): Promise<void>;
    noiseScale: number;
    amplitude: number;
    paused: boolean;
    private _preFrame;
    private _noise;
    protected _makeRoughGround(geometry: BufferGeometry, scl?: number, amp?: number): void;
}
//# sourceMappingURL=WaveGroundPlugin.d.ts.map