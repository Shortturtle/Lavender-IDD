import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { IViewerPluginAsync } from 'webgi/interfaces';
import { AssetManagerPlugin } from 'webgi/extras/asset_manager/AssetManagerPlugin';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export declare class GLTFSpecGlossinessConverterPlugin extends AViewerPlugin<''> implements IViewerPluginAsync {
    enabled: boolean;
    static readonly PluginType = "GLTFSpecGlossinessConverterPlugin";
    toJSON: any;
    dependencies: (typeof AssetManagerPlugin)[];
    /**
     * Whether to show the confirmation dialog when loading a GLTF file with specular glossiness materials.
     * If set to false, the conversion will be done automatically without confirmation.
     * To disable the conversion entirely, disable the plugin.
     */
    confirm: boolean;
    /**
     * The message to show in the confirmation dialog when loading a GLTF file with specular glossiness materials.
     */
    confirmMessage: string;
    private _loaderCreate;
    constructor();
    onAdded(v: ViewerApp): Promise<void>;
    onRemove(v: ViewerApp): Promise<void>;
}
//# sourceMappingURL=GLTFSpecGlossinessConverterPlugin.d.ts.map