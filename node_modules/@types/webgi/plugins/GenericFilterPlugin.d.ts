import { IFilter, IPass, IPassID, IViewerPluginAsync } from 'webgi/interfaces';
import { AViewerPlugin } from 'webgi/core/AViewerPlugin';
import { ViewerApp } from 'webgi/viewer/ViewerApp';
export declare abstract class GenericFilterPlugin<T extends IPass, TPassId extends IPassID, TEvent extends string> extends AViewerPlugin<TEvent> implements IViewerPluginAsync {
    abstract passId: TPassId;
    protected _pass?: IFilter<T, TPassId>;
    abstract passCtor(v: ViewerApp): T;
    /**
     * This function is called every frame before composer render, if this pass is being used in the pipeline
     * @param v
     * @protected
     */
    protected _update(v: ViewerApp): boolean;
    protected _afterFilters?: IPassID[];
    protected _beforeFilters?: IPassID[];
    protected _requiredFilters?: IPassID[];
    private _enabledTemp;
    get enabled(): boolean;
    set enabled(value: boolean);
    constructor();
    onAdded(viewer: ViewerApp): Promise<void>;
    onRemove(viewer: ViewerApp): Promise<void>;
    get pass(): IFilter<T, TPassId> | undefined;
    toJSON(meta?: any): any;
    fromJSON(data: any, meta?: any): this | null;
}
//# sourceMappingURL=GenericFilterPlugin.d.ts.map